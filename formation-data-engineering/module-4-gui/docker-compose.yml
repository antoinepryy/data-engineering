version: '3.8'

services:
  gui:
    build: .
    container_name: module4-gui
    ports:
      - "8502:8501"
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
